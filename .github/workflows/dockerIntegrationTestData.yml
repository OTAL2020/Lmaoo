name: Integration Test using Docker Compose

on:
  pull_request:
    branches: [ develop ]

jobs:
  build:
    runs-on: lmaoodev
    steps:
    - name: Run container
      run: docker-compose -f docker-compose-Integration.yml